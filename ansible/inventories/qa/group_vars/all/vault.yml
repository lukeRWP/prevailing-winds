---
# QA environment secrets — encrypt with: ansible-vault encrypt vault.yml
# Copy structure from dev/group_vars/all/vault.yml and set QA-specific values

vault_mysql_root_password: "CHANGE_ME"
vault_mysql_password: "CHANGE_ME"
vault_mysql_ssl_password: "CHANGE_ME"
vault_s3_access_key: "CHANGE_ME"
vault_s3_secret_key: "CHANGE_ME"
